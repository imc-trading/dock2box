#!ipxe

:begin
prompt --key 0x02 --timeout 2000 "Press Ctrl-B for the iPXE shell..." || goto no_shell
shell
exit

:no_shell
dhcp || goto begin
chain {{server_uri}}/pxe/${netX/mac}?ipv4=${netX/ip}&netmask=${netX/netmask}&gateway=${netX/gateway}&serial=${serial:uristring} || goto no_shell
